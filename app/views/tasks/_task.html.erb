<tr id=<%= "task_#{task.id}" %>>
  <td><%= link_to truncate(task.name), task, remote: true %><td>
  <td><%= truncate task.description %></td>
  <td>

    <%= link_to 'Edit', edit_task_path(task), remote: true %>
    <%= link_to '×', task, :confirm => 'Are you sure you want to delete this entry? This is an irreversible action.', class: :delete,  method: :delete, remote: true %>
  </td>
</tr>
